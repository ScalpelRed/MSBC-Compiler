This program does the same as https://github.com/ScalpelRed/MSBVP-Compiler, but for images.

Sadly, I've lost the shader itself (it won't work with MSBVP version), but I'll probably restore it someday.
